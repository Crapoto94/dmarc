server {
    listen 3200;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location / {
        try_files $uri /index.html;
    }

    location /api/ {
        proxy_pass http://backend:3201;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass_header Set-Cookie;
    }
}

# HTTPS - décommente et configure avec tes certificats
# server {
#     listen 443 ssl http2;
#     server_name dmarc.votredomaine.fr;
#
#     ssl_certificate /etc/letsencrypt/live/dmarc.votredomaine.fr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dmarc.votredomaine.fr/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#
#     root /usr/share/nginx/html;
#     index index.html;
#
#     location / {
#         try_files $uri /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://backend:3201;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_pass_header Set-Cookie;
#     }
# }
